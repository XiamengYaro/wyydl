# 网易云歌单定时同步方案(fnOS NAS)

> 项目代号:`wyydl` · 编写日期:2026-08-28
> 目标:定时把指定的网易云音乐歌单同步到 NAS 指定文件夹,自动以账号可用的最高音质下载,落地为带完整标签(封面/歌词)的普通音频文件,可直接被 Jellyfin/Emby/车机/手机播放器使用。

---

## 1. 需求归纳

| # | 需求 | 方案落点 |
|---|------|----------|
| 1 | 定时同步网易云歌单到文件夹 | 同步器内置调度器(cron),多歌单配置 |
| 2 | 下载到本地 | 流式下载 + 断点容错 + MD5 校验 |
| 3 | 自动获取最高版本(音质) | 基于 `privilege.maxBrLevel` / `dlLevel` 的音质协商,逐级回退 |
| 4 | 使用 api-enhanced 作为 API 服务 | Docker 容器 `moefurina/ncm-api` |
| 5 | NCM 格式转换能力 | 集成 `ncmdump-go` 二进制,作为"官方客户端下载文件"的兜底通道 |
| 6 | 部署在 fnOS | Docker Compose(图形界面或 SSH 均可) |

## 2. 总体架构

```
                       fnOS NAS (Debian, Docker)
 ┌─────────────────────────────────────────────────────────────────┐
 │                                                                 │
 │  ┌──────────────────┐          ┌──────────────────────────────┐ │
 │  │ ncm-api 容器      │  HTTP    │ wyydl-sync 容器(自研,Python) │ │
 │  │ (api-enhanced)   │◄─────────│  · 调度器(APScheduler/cron)  │ │
 │  │ :3000 仅内网      │          │  · 歌单拉取/音质协商          │ │
 │  │                  │          │  · 下载/校验/打标签           │ │
 │  └────────┬─────────┘          │  · NCM 兜底转换(ncmdump-go) │ │
 │           │                    └──────┬───────────┬───────────┘ │
 │           ▼                           │           │             │
 │    music.163.com                 /vol1/media/music  /vol1/wyydl │
 │    (直连,带 MUSIC_U)             ├─ 歌单A/*.flac     ├─ config/  │
 │                                  ├─ 歌单B/*.flac     ├─ db/      │
 │                                  ├─ _ncm_inbox/      └─ logs/    │
 │                                  └─ _trash/                     │
 └─────────────────────────────────────────────────────────────────┘
```

三个组成部分:

1. **ncm-api 容器**(第三方 API 服务,`moefurina/ncm-api:latest`):负责登录态、歌单、取流地址、歌词等一切网易云交互。
2. **wyydl-sync 容器**(本次要写的程序):调度、同步逻辑、下载、写标签、NCM 兜底转换。内嵌 `ncmdump-go` Linux 二进制(官方发布 v1.7.5 提供 linux_amd64 / linux_arm64,无 Docker 镜像,直接打进镜像最省事)。
3. **数据目录**:配置、状态库(SQLite)、日志、音乐输出全部落在 `/vol1` 存储卷上,容器无状态、可随时重建。

## 3. 关键设计

### 3.1 登录与凭证(推荐二维码登录)

网易云已对密码登录加强风控,方案采用**二维码登录 + MUSIC_U Cookie**,一次性操作、长期有效:

1. 首次配置时执行 `docker exec -it wyydl-sync python -m wyydl.qrlogin`;
2. 程序依次调 `/login/qr/key` → `/login/qr/create?key=xxx&qrimg=true`(带时间戳防缓存)→ 轮询 `/login/qr/check?key=xxx`;
3. 终端直接打印 ASCII 二维码(用 `qrcode` 库),手机网易云 App 扫码确认,`803` 状态返回 `cookie`;
4. 提取 `MUSIC_U=...` 存入 `/config/secret.yaml`(权限 600),全程不经过第三方。

运维要点:
- 每次同步前先调 `/login/status` 检查凭证;失效(301/502)则发通知提醒重新扫码。
- 二维码登录的 cookie 不支持 `/login/refresh` 刷新,过期只能重扫——所以要有通知渠道。
- 所有带 cookie 的请求统一附加 `os=pc`(杜比等音质必须,普通音质带上也无副作用);cookie 通过 `?cookie=<urlencoded>` 传给 ncm-api。
- **不要频繁调登录接口**;`/login/qr/*` 调用必须带 `timestamp` 参数防 2 分钟缓存。

### 3.2 音质协商:自动获取"最高版本"

接口:`GET /song/url/v1?id=<id>&level=<level>`,level 档位:`standard → higher → exhigh → lossless → hires → jyeffect → dolby → vivid → jymaster → sky`。

单纯请求最高档 `jymaster`(超清母带)需要 SVIP 且部分音质对设备有要求,盲请求容易拿到试听片段。因此采用**三重协商**:

```
目标音质 = min( 配置上限(默认 jymaster),
               歌曲实际最高音质 privilege.maxBrLevel,
               账号可用音质 privilege.dlLevel )
```

1. 批量调 `/song/detail?ids=...`(每批 50 首),读 `privilege` 字段:
   - `maxBrLevel`:这首歌服务器上有什么档;
   - `dlLevel`:当前账号能拿到什么档;
   - `hr / sq / h / m / l`:各档文件是否存在。
2. 按目标档请求 `/song/url/v1?level=<目标>`(支持逗号分隔批量 id);
3. 逐条校验返回:`url` 非空、`freeTrialInfo` 为空(非试听)、`size`/`md5` 与下载结果比对;若失败**逐级降档重试**(jymaster→hires→lossless→exhigh→standard),仍失败标记跳过;
4. 下载完成后按 `type` 字段确定扩展名(flac/mp3/aac),校验文件大小 + MD5。

容器侧配套环境变量(已在镜像内默认开启,写入 compose 显式声明):`ENABLE_FLAC=true`、`SELECT_MAX_BR=true`(无损时选最高码率)、`ENABLE_GENERAL_UNBLOCK=true`(解灰兜底,可关)。

### 3.3 同步流程(增量)

```
每轮(默认每天 04:00):
1. 登录态检查(/login/status)
2. 对每个配置的歌单:
   a. /playlist/detail?id= 取元数据 + 完整 trackIds(>10 首需此步)
   b. /playlist/track/all?id=&limit=&offset= 分页取全量曲目(500/页)
   c. 与 SQLite 状态库 diff:
      · 新增曲目        → 下载
      · 音质可升级曲目   → 可选重新下载(配置开关,默认开)
      · 已移除曲目       → 默认移入 _trash/(镜像模式可配为直接删)
3. 新曲目:音质协商 → 下载 → mutagen 写标签 → 移入歌单目录
4. 标签与歌词:
   · 标题/歌手/专辑/音轨号/碟号:/song/detail
   · 封面:专辑封面 URL(?param=1000y1000)嵌入 + 落盘 cover.jpg
   · 歌词:/lyric?id= 存同名 .lrc;可选 /lyric/new 取逐字歌词
   · NFO:单曲 <歌名>.nfo(标题/歌手/专辑/音轨/时长/流派/网易云 ID);归档布局下另写 album.nfo、artist.nfo(调 /album 按专辑缓存,含流派/厂牌/发行日期/简介/曲目列表)
5. 汇总通知(新增/升级/失败清单)
```

- **命名模板**可配,默认 `02. 周杰伦 - 晴天.flac`(序号保持歌单顺序,车机/U盘友好);可选 `歌手/专辑` 归档模式。
- 文件名非法字符清洗、重名自动加 `(Live版ID后缀)` 防覆盖。
- 下载并发 3,URL 请求间隔 1–3s 随机抖动,降低风控概率。

### 3.4 NCM 兜底通道(ncmdump-go)

API 直连拿不到完整文件的场景(VIP 失效、下架、账号无会员但官方客户端曾下载过),用**官方客户端下载的 .ncm 文件**补齐:

- 监视目录 `/vol1/media/music/_ncm_inbox/`:PC 端网易云"下载目录"设到这里的 SMB/NFS 共享,或手动丢文件进去;
- 每轮同步前执行 `ncmdump-go -d /inbox -o /tmp/converted -r`(自动解密出 flac/mp3,并联网补嵌 3.x 缺失的封面);
- 转换产物经 mutagen 校验/补写标签后按元数据归档;inbox 内原文件处理成功后移入 `_inbox_done/`,失败留在原地等下轮。

这样 ncmdump-go 与 API 主通道互补:API 通道自动化程度高,NCM 通道保证"客户端能下的都能入库"。

### 3.5 Web 控制面板(已确认需求)

同步容器内置 FastAPI 面板(默认 `:8286`,可配 `web.token` 访问令牌):

- **总览**:登录态、下次运行时间、当前同步进度(阶段/计数)、最近运行统计;
- **歌单管理**:面板直接按 ID 添加/移除歌单(添加即同步一轮),查看每首歌的入库状态与音质;
- **扫码登录**:面板内展示二维码并轮询状态,`MUSIC_U` 自动保存,无需进终端;
- **运维**:YAML 配置在线编辑(保存即生效,含 cron 热更新)、日志尾迹、飞书通知测试、手动触发整轮/单歌单同步。

### 3.6 风控与合规

- 请求全部发往**自建 ncm-api**,网易云侧只看到家里宽带 IP(国内家宽,无需 realIP;海外网络需加 `realIP`/`randomCNIP` 参数或 `ENABLE_RANDOM_CN_IP=true`);
- 登录接口绝不自动化重试;单轮同步对 URL 接口做限速;
- 仅限个人备份使用,下载内容不对外分享——文档与程序 README 需注明"仅供学习,尊重版权,勿商用"。

## 4. 目录规划(fnOS)

```
/vol1/media/music/            # fnOS 共享文件夹(多媒体库根目录)
├── 我的喜欢的音乐/            # 每个歌单一个子目录
├── 每日推荐/
├── _ncm_inbox/               # NCM 投放目录
├── _inbox_done/
└── _trash/                   # 同步删除的文件暂存(保留 30 天后可手动清)

/vol1/appdata/wyydl/          # 程序数据(fnOS 共享文件夹,建议不开 SMB)
├── config/config.yaml        # 主配置
├── config/secret.yaml        # MUSIC_U(600 权限)
├── db/state.sqlite3
└── logs/
```

## 5. 部署设计(fnOS)

### 5.1 前置条件

1. fnOS 应用中心安装 **Docker** 应用;
2. (可选)开启 SSH:设置 → 远程 → SSH,以下命令也可在 fnOS Docker 界面的 **Compose/项目** 功能里粘贴执行;
3. 在 fnOS 文件管理中建好上述两个共享文件夹。

### 5.2 docker-compose.yml

```yaml
# /vol1/appdata/wyydl/docker-compose.yml
services:
  ncm-api:
    image: moefurina/ncm-api:latest
    container_name: ncm-api
    restart: unless-stopped
    environment:
      # 容器内 request 库会读代理变量,必须清空,否则请求全部失败
      http_proxy: ""
      https_proxy: ""
      HTTP_PROXY: ""
      HTTPS_PROXY: ""
      no_proxy: ""
      NO_PROXY: ""
      ENABLE_FLAC: "true"
      SELECT_MAX_BR: "true"
      ENABLE_GENERAL_UNBLOCK: "true"
    ports:
      - "127.0.0.1:3000:3000"   # 仅本机/SSH 隧道访问,不暴露局域网
    networks: [wyydl]

  wyydl-sync:
    build: ./sync               # 自研程序,Dockerfile 见 5.3
    container_name: wyydl-sync
    restart: unless-stopped
    depends_on: [ncm-api]
    environment:
      - TZ=Asia/Shanghai
      - NCM_API=http://ncm-api:3000
    ports:
      - "8286:8286"             # Web 控制面板
    volumes:
      - /vol1/appdata/wyydl/config:/config
      - /vol1/appdata/wyydl/db:/db
      - /vol1/appdata/wyydl/logs:/logs
      - /vol1/media/music:/music
    networks: [wyydl]

networks:
  wyydl:
```

部署方式二选一:
- **图形界面**:fnOS Docker → 项目/Compose → 新建 → 指向 `/vol1/appdata/wyydl/` 粘贴上述 yaml;
- **SSH**:`cd /vol1/appdata/wyydl && docker compose up -d --build`。

### 5.3 sync 镜像(Dockerfile)

```dockerfile
FROM python:3.12-slim
ARG NCMDUMP_VER=1.7.5
ARG TARGETARCH          # amd64 / arm64,fnOS x86 与 ARM 均可
RUN apt-get update && apt-get install -y --no-install-recommends curl ca-certificates \
 && curl -L https://git.taurusxin.com/taurusxin/ncmdump-go/releases/download/v${NCMDUMP_VER}/ncmdump-go_${NCMDUMP_VER}_linux_${TARGETARCH}.tar.gz \
    | tar xz -C /usr/local/bin ncmdump-go && chmod +x /usr/local/bin/ncmdump-go \
 && apt-get purge -y curl && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY wyydl/ ./wyydl/
CMD ["python", "-m", "wyydl.main"]
```

依赖(`requirements.txt`):`httpx`、`mutagen`、`qrcode`、`apscheduler`、`pyyaml`。

### 5.4 定时方式

程序内置 APScheduler(读 `config.yaml` 的 `schedule: "0 4 * * *"`),容器常驻,不依赖 fnOS 计划任务——换 NAS/重装应用不影响。
备选:fnOS 计划任务执行 `docker exec wyydl-sync python -m wyydl.main --once`(适合希望容器按需启停省资源的场景)。

手动触发:`docker exec -it wyydl-sync python -m wyydl.main --once --playlist <id>`。

### 5.5 config.yaml 设计

```yaml
schedule: "0 4 * * *"          # 每天凌晨 4 点
api_base: http://ncm-api:3000
layout: archive                # archive=歌手/专辑归档+m3u8 | playlist=按歌单分文件夹
quality:
  chain: [jymaster, hires, lossless, exhigh, standard]   # VIP/SVIP 自动吃满
  upgrade_existing: true       # 出现更高音质时重新下载
playlists:
  - { id: 24381616, name: 可选自定义 }   # name 留空则用接口返回的歌单名
lyrics: { lrc: true, embed: true }
mirror: false                  # 歌单移除的歌曲是否移入 _trash(archive 布局默认不删文件)
notify:
  type: feishu                 # 飞书自定义机器人 webhook
  url: https://open.feishu.cn/open-apis/bot/v2/hook/xxxx
  secret: "加签密钥(机器人开启签名校验时填)"
web: { enabled: true, port: 8286, token: "" }
ncm_inbox: true
limits:
  download_concurrency: 3
  api_delay: [1.0, 3.0]        # 随机区间秒
```

## 6. 程序模块划分

```
wyydl/
├── main.py        # 入口:--once / --playlist / 守护(Web+调度)
├── config.py      # 配置加载、secret 管理
├── api.py         # ncm-api 客户端(登录检查/歌单/详情/取流/歌词,统一限速)
├── quality.py     # 音质协商(maxBrLevel × dlLevel × 上限 → 降档链)
├── downloader.py  # 流式下载、MD5/大小校验、失败降档重试
├── tagger.py      # mutagen 写 flac/mp3/m4a 标签 + 封面 + .lrc
├── ncm.py         # ncmdump-go 调用与 inbox 轮转
├── state.py       # SQLite:曲目↔文件↔音质↔时间戳
├── syncer.py      # 同步引擎(增量 diff/批量取流/并发下载/m3u8/清理)
├── qrlogin.py     # 扫码登录 CLI(ASCII 二维码)
├── notify.py      # 飞书 webhook(可加签)
└── web.py + static/index.html   # Web 控制面板(FastAPI)
```

## 7. 风险与已知限制

| 风险 | 影响 | 对策 |
|------|------|------|
| 网易接口风控(高频/IP 异常) | 拉歌单/取流失败 | 限速 + 抖动 + 缓存;避免自动化登录 |
| MUSIC_U 过期 | 同步失败 | 每轮预检 + 通知重扫 |
| VIP 音质随会员状态浮动 | 实际音质低于预期 | 协商机制保证拿到"当前账号可得最高",并在通知中列明降档曲目 |
| 灰色/下架歌曲无 URL | 缺文件 | `ENABLE_GENERAL_UNBLOCK` 解灰兜底 + NCM inbox 通道 + 失败清单 |
| 代理环境变量污染 ncm-api 容器 | API 全部请求报错 | compose 中显式清空(见 5.2,官方 README 强调) |
| fnOS 大版本 Docker 界面差异 | 操作路径不同 | 提供 SSH + compose 的等价路径,以 fnOS 实际界面为准 |

## 8. 实施计划

1. **阶段一(骨架)**:compose 跑通 ncm-api;qrlogin 拿到 cookie;`--once` 拉取单个歌单列表打印。
2. **阶段二(主链路)**:音质协商 + 下载 + mutagen 打标签 + 状态库增量。
3. **阶段三(完整性)**:歌词/封面、NCM inbox、_trash 镜像、通知。
4. **阶段四(打磨)**:音质升级重下、失败重试策略、日志轮转、README 与 fnOS 图文部署说明。

## 9. 已确认决策(2026-08-28)

1. **NAS 架构**:x86_64(Dockerfile 保留 TARGETARCH 自适应,兼容日后换 ARM)。
2. **账号**:有 VIP,`quality.chain` 上限 `jymaster`,有 SVIP 权益时自动吃满母带/Hi-Res。
3. **文件组织**:**按歌手/专辑归档**(`layout: archive`),同一首歌只存一份,每个歌单生成同名 `.m3u8` 播放列表;`playlist` 布局保留为可选项。
4. **通知**:飞书自定义机器人 webhook(支持加签 secret);面板提供测试按钮。
5. **Web 面板**:作为标配功能实现(见 3.5),端口 8286。
